<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Ejercicio1._02.Pageresultado">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding nombre}" TextColor="Black" FontSize="Body" Background="#ccd7ff" Margin="20"/>

            <Label Text="{Binding apellidos}" TextColor="Black" FontSize="Body" Background="#ccd7ff" Margin="20"/>

            <Label Text="{Binding edad}" TextColor="Black" FontSize="Body" Background="#ccd7ff" Margin="20"/>

            <Label Text="{Binding correo}" TextColor="Black" FontSize="Body" Background="#ccd7ff" Margin="20"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>